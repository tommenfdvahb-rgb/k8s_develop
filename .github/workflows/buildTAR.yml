name: Build K8s Offline Package

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        arch: [amd64, arm64]
        pkg: [rpm, deb]

    env:
      SEALOS_VERSION: v5.1.1
      K8S_VERSION: v1.30.14
      CALICO_VERSION: v3.27.4
      INGRESS_VERSION: v1.11.3
      LONGHORN_VERSION: v1.6.4

    steps:
      - uses: actions/checkout@v4

      # ======================
      # 安装 sealos
      # ======================
      - name: Install sealos
        run: |
          set -euo pipefail
          # GitHub Actions runners are typically amd64; use runner arch so we can execute sealos locally.
          SEALOS_ARCH="$(uname -m)"
          case "$SEALOS_ARCH" in
            x86_64) SEALOS_ARCH=amd64 ;;
            aarch64|arm64) SEALOS_ARCH=arm64 ;;
            *) echo "Unsupported runner arch: $SEALOS_ARCH" >&2 ; exit 1 ;;
          esac

          SEALOS_PKG="sealos_${SEALOS_VERSION#v}_linux_${SEALOS_ARCH}.tar.gz"
          curl -fL --retry 3 --retry-delay 2 -o "$SEALOS_PKG" \
            "https://github.com/labring/sealos/releases/download/${SEALOS_VERSION}/${SEALOS_PKG}"
          tar -xvf "$SEALOS_PKG"
          install -m 0755 sealos /usr/local/bin/sealos
          sealos version

      # ======================
      # 构建 K8s 镜像列表
      # ======================
      - name: Prepare k8s images
        run: |
          cat <<EOF > k8s-images.txt
          registry.cn-shanghai.aliyuncs.com/labring/kubernetes:${K8S_VERSION}
          registry.cn-shanghai.aliyuncs.com/labring/calico:${CALICO_VERSION}
          registry.cn-shanghai.aliyuncs.com/labring/ingress-nginx:${INGRESS_VERSION}
          registry.cn-shanghai.aliyuncs.com/labring/longhorn:${LONGHORN_VERSION}
          EOF

      # ======================
      # 拉取 & 打包 K8s 镜像
      # ======================
      - name: Build k8s image tar
        run: |
          set -euo pipefail
          mkdir -p tmp
          chmod 1777 tmp
          docker run --rm --privileged \
            -e TARGET_ARCH="${{ matrix.arch }}" \
            -v "$PWD:/work" -v "$PWD/tmp:/tmp" -v "$PWD/tmp:/var/tmp" -w /work ubuntu:22.04 bash -lc '
            set -euo pipefail
            export TMPDIR=/tmp
            apt-get update
            apt-get install -y --no-install-recommends fuse-overlayfs ca-certificates
            export CONTAINERS_STORAGE_DRIVER=vfs
            retry() {
              local max=$1
              shift
              local n=1
              until "$@"; do
                if (( n >= max )); then
                  return 1
                fi
                sleep $((n * 5))
                n=$((n + 1))
              done
            }
            declare -a SUCCESSFUL_IMAGES=()
            mapfile -t K8S_IMAGES < k8s-images.txt
            for img in "${K8S_IMAGES[@]}"; do
              if /work/sealos pull --platform "linux/${TARGET_ARCH}" "$img" 2>/dev/null; then
                SUCCESSFUL_IMAGES+=("$img")
                echo "Pulled $img"
              else
                echo "Skipped $img (not available for ${TARGET_ARCH})"
              fi
            done
            if (( ${#SUCCESSFUL_IMAGES[@]} > 0 )); then
              retry 3 /work/sealos save -o k8s-images.tar "${SUCCESSFUL_IMAGES[@]}"
            else
              echo "No images to save for ${TARGET_ARCH}"
              touch k8s-images.tar
            fi
          '

      # ======================
      # RPM 构建
      # ======================
      - name: Build RPM packages
        if: matrix.pkg == 'rpm'
        run: |
          docker run --rm -v $PWD:/out centos:7 bash -c "
            yum install -y yum-utils createrepo
            mkdir -p /out/packages/rpm
            yumdownloader --resolve --destdir=/out/packages/rpm \
              conntrack-tools socat ipset iptables \
              nfs-utils iscsi-initiator-utils iproute \
              nmap-ncat tar coreutils util-linux procps-ng openssh-clients
            createrepo /out/packages/rpm
          "

      # ======================
      # DEB 构建
      # ======================
      - name: Build DEB packages
        if: matrix.pkg == 'deb'
        run: |
          docker run --rm -v $PWD:/out ubuntu:22.04 bash -c "
            apt-get update
            apt-get install -y dpkg-dev
            mkdir -p /out/packages/deb
            apt-get --download-only \
              -o Dir::Cache::archives=/out/packages/deb \
              install -y \
              conntrack socat ipset iptables \
              nfs-common open-iscsi \
              iproute2 netcat-openbsd \
              tar coreutils util-linux procps openssh-client
            cd /out/packages/deb
            dpkg-scanpackages . /dev/null | gzip -9c > Packages.gz
          "

      # ======================
      # 打包最终离线包
      # ======================
      - name: Package
        run: |
          mkdir -p release/images release/packages release/bin release/scripts

          # 运行容器时会生成 root 所有的文件，确保我们有权限移动
          sudo chown -R "$(id -u):$(id -g)" packages || true

          mv k8s-images.tar release/images/

          case "${{ matrix.pkg }}" in
            rpm)
              mv packages/rpm release/packages/ || true
              ;;
            deb)
              mv packages/deb release/packages/ || true
              ;;
          esac

          cp install.sh release/scripts/

          tar -czvf k8s-offline-${{ matrix.arch }}-${{ matrix.pkg }}.tar.gz release

      # ======================
      # 发布 Release
      # ======================
      - name: Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: k8s-offline-${{ matrix.arch }}-${{ matrix.pkg }}.tar.gz
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
